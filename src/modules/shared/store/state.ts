import { SharedInitialState } from './contracts'

const sharedInitialState: SharedInitialState = {
  device: {
    isDesktop: true,
    isTablet: false,
    isMobile: false
  }
}

export default sharedInitialState
